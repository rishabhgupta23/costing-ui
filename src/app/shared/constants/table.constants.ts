export enum ColumnType {
    GENERAL,
    INPUT_NUMBER,
    ACTION = 'ACTION'
}

export enum TableActions {
    EDIT,
    DELETE
}

export enum SortIcons {
    ASC = 'ASC',
    DESC = 'DESC',
    DEFAULT = 'DEFAULT-SORT'
}